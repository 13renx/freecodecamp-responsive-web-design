# freecodecamp-responsive-web-design

1. build-a-curriculum (Build a Curriculum Outline)
    - Basic HTML page using several elements (`p`, `h1`, `h2`, `h3`)
2. build-a-cat-photo-app (Build a Cat Photo App)
